{
  "name": "recipe-app-json",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
"start": "json-server --watch backend/db.json --port $PORT"

},
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "engines": {
    "node": "14.x"
  }
}
