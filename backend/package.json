{
  "name": "recipe-app-json",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
   "start": "json-server --watch db.json --port ${PORT:-3000}"

  },
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "engines": {
    "node": "14.x"
  }
}
